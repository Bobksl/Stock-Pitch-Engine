"""Phase 2 — the valuation engine (framework section 4).

Pure calculation: Decimal in, Decimal out, no I/O and no database. Every
figure this package produces is a derived figure in the sense of framework
6.4, and is recomputed at verification time rather than trusted.
"""
